# **************************************************
# Copyright (c) 2025, Mayank Mishra
# **************************************************

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "accelerated-model-architectures"
version = "0.1.0"
description = "A library for efficient kernels and model architectures for a bunch of different hardware accelerators."
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "Mayank Mishra", email = "mayank31398@gmail.com" },
]
dependencies = [
    "ninja==1.13.0",
    "numpy==2.5.1",
    "pyyaml==6.0.3",
]

[project.urls]
Homepage = "https://github.com/open-lm-engine/accelerated-model-architectures"
Repository = "https://github.com/open-lm-engine/accelerated-model-architectures"

[project.optional-dependencies]
dev = [
    "furo==2025.12.19",
    "pre-commit==4.6.0",
    "pytest==9.1.1",
    "pytest-xdist==3.8.0",
    "sphinx==9.1.0",
    "sphinx-autobuild==2025.8.25",
]
cpu = ["torch==2.10.0"]
mps = ["torch==2.10.0"]
cuda = [
    "torch==2.10.0",
    "apache-tvm-ffi==0.1.12",
    "nvidia-cutlass-dsl==4.6.0",
    "tabulate==0.10.0",
]
torch-xla = [
    "equinox==0.13.8",
    "jaxtyping==0.3.11",
    "torch==2.8.0",
    "torch-xla[pallas,tpu]==2.8.0",
    "tabulate==0.10.0",
]
tpu = [
    "equinox==0.13.8",
    "jax[tpu]==0.11.0",
    "jaxtyping==0.3.11",
]

[tool.uv]
conflicts = [
    [
        { extra = "cpu" },
        { extra = "mps" },
        { extra = "cuda" },
        { extra = "torch-xla" },
        { extra = "tpu" },
    ],
]

[tool.hatch.build.targets.wheel]
packages = ["xma"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.isort]
ensure_newline_before_comments = true
force_grid_wrap = 0
include_trailing_comma = true
line_length = 119
lines_after_imports = 2
multi_line_output = 3
use_parentheses = true
